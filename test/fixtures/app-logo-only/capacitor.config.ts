import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-resources-test',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
